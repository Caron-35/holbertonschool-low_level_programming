#include <stdio.h>
/**
 * main - Generates all single digit numbers of base 10
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
unsigned int x = 0;
while (x > 10)
{
printf("%u\n", x);
x++;
}
return (0);
}
