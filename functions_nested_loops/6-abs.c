#include "main.h"
/**
 * _abs - Program the absolute number
 * @int: number variable
 * r : number
 * Return: always the absolute number
 */
int _abs(int r)
{
if (r < 0)
{
return (-r);
}
else
{
return (r);
}
}
